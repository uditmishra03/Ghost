# AWS Configuration# AWS Configuration

# Replace with your AWS Account ID (12-digit number)# Replace with your AWS Account ID (12-digit number)

AWS_ACCOUNT_ID=123456789012AWS_ACCOUNT_ID=123456789012



# Example usage:# Example usage:

# 1. Copy this file to .env: cp .env.example .env# 1. Copy this file to .env: cp .env.example .env

# 2. Update the AWS_ACCOUNT_ID with your actual AWS account number# 2. Update the AWS_ACCOUNT_ID with your actual AWS account number

# 3. Run docker-compose: docker-compose -f docker-compose.production.yml up -d# 3. Run docker-compose: docker-compose -f docker-compose.production.yml up -d



# Note: The example account ID above (123456789012) is a placeholder.# Note: The example account ID above (123456789012) is a placeholder.

# Replace it with your actual 12-digit AWS account ID.# Replace it with your actual 12-digit AWS account ID.